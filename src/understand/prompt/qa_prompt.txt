I'll teach you how to read navigational signs today. There are 2 important components of any sign : the text or symbol corresponding to the location and the arrow describing the direction you need to move in, as if you are standing in front of the board, in order to reach the location.

If you see location names besides these arrows then -- then specify the direction of the arrow as it would look if you were standing perfectly in front of the sign board.
If you see symbols on the navigational sign then use your commonsense understanding of the symbol image and the text around it to convert it to text name for representing as a location name. Use your commonsense understanding to decode such common symbols observed in navigational signs to locations. Don't restrict yourself to the example symbols.

Use Gestalt psychology principles and structured visual layout reasoning to associate arrows, symbols, and text as humans naturally would when reading navigational signs.

Treat navigational signs as a structured visual layout similar to an HTML/CSS grid or grouped information board.

Humans naturally segment signs into visual groups or regions. A destination's associated direction is often determined by its visual grouping rather than nearest-neighbor distance alone.

Pay particular attention to:

1. **Proximity** — text, symbols, and arrows that are spatially close together are likely associated.

2. **Alignment** — destinations, symbols, and arrows aligned horizontally or vertically may belong to the same directional group.

3. **Common Region** — text, symbols, and arrows enclosed within the same visual section, panel, box, or sign grouping are likely related.

4. **Similarity** — visually similar icons, repeated formatting, typography, spacing, or shared colors may indicate grouped destinations. However, color alone should not override stronger cues such as alignment, proximity, or explicit arrow association.

5. **Continuity** — if multiple destinations appear visually connected to a single arrow or grouped region, assume they share the same direction unless strong evidence suggests otherwise. If a single arrow visually governs a group of destinations, symbols, or text items, assign the same direction to all destinations within that visual group unless strong evidence suggests otherwise. Only return multiple directions as a list when the sign clearly shows multiple arrows or multiple directional groups for the same destination. Do not return duplicate directions.

6. **Wrapped / Multi-line Text** — destination text spanning multiple lines should usually be treated as one destination group and associated with the same arrow.

7. **Figure-Ground** — focus primarily on the navigational sign itself and ignore irrelevant background scene clutter.

When associating arrows with destinations, reason about the sign as if it were divided into structured visual containers or grouped sections. Consider both horizontal and vertical grouping. Multiple destinations may share one arrow if they belong to the same visual group. if natural border and grouping exist than it should be more prioritize over than simply proximity. However, if a destination has a clearly visible arrow on the same row or immediately beside it, that row-level arrow takes priority. Use broader grouping only when the row-level arrow association is unclear or missing.

Prioritize human-natural visual grouping over strict nearest-neighbor geometric distance.

Now I will teach you how to read arrows. We need to read the arrows in the frame of the navigation signboard.

If it points north then we say it points to the direction "straight". 
"straight" arrows would be single line segment with the ^ pointing up
"straight" arrows are vertical ( | )

If it points to south then we say it points to the direction "down".
"down" arrow would be a single line segment with v pointing down
"down" arrows are vertical ( | )

If it points east wrt to the board, we say it points to "right". 
"right" arrows would be single line segment with a > pointing right 
"right" arrows are horizontal ( - )

If it points west wrt to the board we say it points to "left"
"left" arrows would be single line segment with the < pointing left 
"left" arrows are horizontal ( - )

If it points diagonally north-east wrt to the board we say it points to "straight-right"
"straight-right" arrows would be single line with ^ pointing "north-east"
"straight-right" arrows are diagonal ( / )

If it points diagonally south-west wrt to the board we say it points to "down-left"
"down-left" arrows would be a single line with v pointing "south-west"
"down-left" arrows are diagonal ( / )
Use the arrowhead direction to determine diagonal arrows. Do not rely only on whether the line visually looks like "/" or "\".

If it points diagonally north-west wrt to the board we say it points to "straight-left"
"straight-left" arrows would be single line with ^ pointing "north-west"
"straight-left" arrows are diagonal ( \ )

If it points diagonally south-east wrt to the board we say it points to "down-right"
"down-right" arrows would be a single line with v pointing "south-east"
"down-right" arrows are diagonal ( \ )
Use the arrowhead direction to determine diagonal arrows. Do not rely only on whether the line visually looks like "/" or "\".

If it indicates a U-turn wrt to the board we say it points to the direction "u-turn".
"u-turn" arrows are curved arrows that indicate turning back toward the direction from which the viewer came.
Use the arrowhead together with the arrow path to determine whether an arrow represents a U-turn maneuver.

Some arrows only have the arrowhead, use your commonsense to figure the direction based on where the arrowhead points to.

If there is some information on the navigation board with NO ARROWS associated with it, then respond the arrow direction as "locational" for that location text. These type of locational information tell us about where we are currently.

I want accuracy.Focus very carefully on the naviagtional board. SEE VERY CAREFULLY all text name of symbol or location name and their arrow associations. The output direction should be from this list ['left','right','straight','straight-right','straight-left','down-left','down-right','locational', 'down', 'unknown', 'uncertain', 'u-turn']

You will be given user questions about locations shown on the navigational sign.

Answer each question using directions from:
['left','right','straight','straight-right','straight-left','down-left','down-right','locational', 'down', 'unknown', 'uncertain', 'u-turn']

If uncertain, answer:
"uncertain"

If the destination is not in the image provided, please answer: "unknown"

Some destinations may have multiple valid directions on the sign. If a destination is associated with multiple arrows or appears in multiple directional groups, return a list of directions.

Icons doesn't have one singular name, it may have multiple names. Use your commonsense to figure out the user meant based on common open vocabularies. For instance, 'restroom', 'bathroom', and 'toliet' can all refer to the same icon.

Platforms refers to a number when navigation the subway. For instance 'Platform 2', look for the number 2 on the navigation sign.

For example:

{
    "Where is PASSENGER LIFTS?": ["straight-right", "straight-left"]
}

If there is only one valid direction, return a single string.

Return ONLY valid JSON in this exact format:

{
    "Where is MRT?": "straight",
    "Where is TOILET?": "right",
    "Where is PASSENGER LIFTS?": ["straight-right", "straight-left"]
}

No explanations.
No extra text.
Only return JSON.